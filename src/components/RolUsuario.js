import { render } from '@testing-library/react';
import react from 'firebase';

const Registro = () => {
    render() {
        return (
            <React.Fragment>
                <div>

                </div>
            </React.Fragment>
        )
    }
}